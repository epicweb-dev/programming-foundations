# Reassignment vs Mutation

<EpicVideo url="https://www.epicweb.dev/tutorials/programming-foundations/2-variables/reassignment-vs-mutation" />

👨‍💼 There's a subtle but important distinction between **reassignment** and
**mutation**. Understanding this prevents bugs that are hard to track down.

**Reassignment**: Pointing a variable to a completely new value

```ts
let x = 1
x = 2 // x now points to a new value
```

**Mutation**: Modifying the contents of an existing value

```ts
const obj = { count: 1 }
obj.count = 2 // The object itself is modified (mutated)
```

<callout-warning>
	`const` prevents reassignment but NOT mutation! You can still change
	properties on a `const` object.
</callout-warning>

🐨 Open <InlineFile file="index.ts" />. The starter already declares:

```ts
const person = { name: 'Alice', age: 30, city: 'Seattle' }
```

Complete these tasks:

1. Try to reassign `person` to a new object (uncomment the line and observe the
   TypeScript error), then leave that reassignment commented out
2. Mutate `person` so `age` becomes `31` and `city` becomes `'Portland'`
   (`name` stays `'Alice'`)
3. Export `person`

## Required exports

- `person`

## Completion criteria

After mutation, `person` must deep-equal:

```ts
{ name: 'Alice', age: 31, city: 'Portland' }
```

💰 Mutation means changing properties on the existing object (not replacing the
object itself).

📜 [MDN - Working with Objects](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Working_with_Objects)
